// SvelteKit app-namespace types. Empty scaffold — this library doesn't
// carry app-shape state; consumer projects declare their own.

// biome-ignore lint/style/useNamingConvention: SvelteKit convention
declare namespace App {
	// interface Locals {}
	// interface PageData {}
	// interface PageState {}
	// interface Platform {}
	// interface Error {}
}
